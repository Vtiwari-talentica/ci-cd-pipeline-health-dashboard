# Environment Variables for CI/CD Pipeline Health Dashboard
# Copy this file to .env and configure with your actual values

# Backend Configuration
BACKEND_PORT=8001
ALLOW_ORIGINS=http://localhost:5173

# Frontend Configuration  
FRONTEND_PORT=5173
VITE_BACKEND_URL=http://localhost:8001

# Database Configuration
SQLALCHEMY_DATABASE_URL=sqlite:///./dashboard.db

# ==============================================
# ALERTING CONFIGURATION (REQUIRED FUNCTIONALITY)
# ==============================================

# Slack Alerting (Primary option - webhook based)
# Get webhook URL from: https://api.slack.com/apps -> Your App -> Incoming Webhooks
# Format: https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXXXXXX
ALERT_SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Email Alerting (Secondary option - SMTP based)
ALERT_EMAIL_FROM=alerts@yourdomain.com
ALERT_EMAIL_TO=team@yourdomain.com

# SMTP Configuration (Required for email alerts)
# Gmail Example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Other SMTP Provider Examples:
# Outlook: SMTP_HOST=smtp-mail.outlook.com
# Yahoo: SMTP_HOST=smtp.mail.yahoo.com
# SendGrid: SMTP_HOST=smtp.sendgrid.net
# Amazon SES: SMTP_HOST=email-smtp.us-east-1.amazonaws.com
